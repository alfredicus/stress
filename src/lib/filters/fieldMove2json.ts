import { filter as F } from "./filter2json"

namespace filter {

    export function fieldMove2json(buffer: string, {separator = ';'}:{separator?: string}={}): F.JSONValue {
        return {}
    }

}
